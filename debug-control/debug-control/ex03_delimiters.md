## Version A - with delimiters:

Answer using only the text inside the triple backticks below. If the answer is not in the text, say so.

The `snapctl` tool takes screenshots from the command line. Running `snapctl capture` saves a PNG to the current folder. You can change the output format with `--format`, which accepts `png`, `jpg`, or `webp`. By default, screenshots are saved to `./screenshots`. The tool requires Node.js 18 or higher.

- - - Question: Where are screenshots saved by default?

## Version B - without delimiters:

Answer using only the text below. If the answer is not in the text, say so.

The snapctl tool takes screenshots from the command line. Running snapctl capture saves a PNG to the current folder. You can change the output format with --format, which accepts png, jpg, or webp. By default, screenshots are saved to ./screenshots. The tool requires Node.js 18 or higher.

- - - Question: Where are screenshots saved by default?


## output A

By default, screenshots are saved to `./screenshots`.

## output B

By default, screenshots are saved to `./screenshots`.

## 4: 
No difference here - both versions answered identically, since the question was simple enough that the lack of delimiters didn't cause any drift.