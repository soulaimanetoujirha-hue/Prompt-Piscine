## 1: Write a factual question, then provide a short passage (a few sentences of documentation, a README excerpt, or notes) that contains the answer.
Answer using only the text provided below. If the answer is not in the text, say so.

Text:
The snapctl tool takes screenshots from the command line. Running snapctl capture saves a PNG to the current folder. You can change the output format with --format, which accepts png, jpg, or webp. By default, screenshots are saved to ./screenshots. The tool requires Node.js 18 or higher.

## 2: Add an instruction
Question: What image formats does --format accept?

`output:` The `--format` flag accepts **png**, **jpg**, or **webp**.
-----------------------------------------------------------------------
Answer using only the text provided below. If the answer is not in the text, say so.

Text:
The snapctl tool takes screenshots from the command line. Running snapctl capture saves a PNG to the current folder. You can change the output format with --format, which accepts png, jpg, or webp. By default, screenshots are saved to ./screenshots. The tool requires Node.js 18 or higher.

## 3: and one whose answer is not
Question: What keyboard shortcut does snapctl use to capture a screenshot?


`output:` The provided text does not mention what keyboard shortcut `snapctl` uses.



## 4: did the model stay inside the provided text, including refusing the one it could not answer?
Yes - the model stayed inside the provided text, correctly answering the first question from the passage and refusing the second since the keyboard shortcut wasn't mentioned.